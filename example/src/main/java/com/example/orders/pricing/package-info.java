/**
 * Legacy pricing code, not yet annotated with JSpecify nullness markers.
 *
 * <p>Delete it and run {@code ./gradlew build} to make NullMarked Gradle Plugin generate one with
 * {@code @NullMarked} annotation and see errors from NullAway.
 */
package com.example.orders.pricing;
